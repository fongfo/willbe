PORT=4000
NODE_ENV=development

# Database (PostgreSQL) — added when WB-11 persistence lands
# DATABASE_URL=postgres://user:password@localhost:5432/willbe

# Redis — added when caching/session needs land
# REDIS_URL=redis://localhost:6379
