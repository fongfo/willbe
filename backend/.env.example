PORT=4000
NODE_ENV=development

# Matches docker-compose.yml at the repo root (`docker compose up -d`)
DATABASE_URL="postgresql://willbe:willbe_dev@localhost:5432/willbe?schema=public"
REDIS_URL="redis://localhost:6379"
