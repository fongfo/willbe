import PlaceholderScreen from '../screens/PlaceholderScreen';

export default function ConsentScreen() {
  return (
    <PlaceholderScreen
      title="Consent"
      subtitle="PDPA consent flow — copy pending legal review (WB-2)."
    />
  );
}
